(function() {
  // Immediately invoked anonymous function

  // Call the function topMovies()
  topMovies();

  // Call the function comingMovies()
  comingMovies();

  // Call the function topPeople()
  topPeople();

  // Call the function movieFilter()
  movieFilter();
})();
